{
  "name": "transliteration-extension",
  "version": "1.0.0",
  "description": "Browser extension for transliterating text in any editable field",
  "scripts": {
    "build": "node build.js",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.246",
    "rimraf": "^5.0.0",
    "typescript": "^5.1.3"
  }
}
